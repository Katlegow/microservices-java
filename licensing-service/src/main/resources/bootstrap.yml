
spring:
  application:
    name: licensingservice
  profiles:
    active: default
  cloud:
    config:
      enabled: true
  sql:
    init:
      mode: always
  main:
    allow-circular-references: true